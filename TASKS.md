# TASKS.md - مهام التطوير التفصيلية لمشروع Prime Scope

دليل شامل للمهام البرمجية مقسمة حسب التخصص والمسؤوليات لتسهيل عملية التتبع والإنجاز.

---

## 🎨 مهام الواجهة الأمامية (Frontend Tasks)

### 1. الإعداد والتأسيس
- [ ] تهيئة مشروع React.js جديد باستخدام Vite.
- [ ] تثبيت وتعديل Tailwind CSS وتأكيد ملف التهيئة `tailwind.config.js`.
- [ ] إنشاء هيكل المجلدات (Components, Pages, Assets, Hooks, Context, Services).
- [ ] إعداد موجه مسارات الموقع (React Router DOM) وتجهيز المسارات الأساسية.

### 2. بناء المكونات والصفحات
- [ ] تصميم **الصفحة الرئيسية**: قسم الترحيب، نبذة عن الرخام والحجر والبازلت، كروت الفئات الرئيسية.
- [ ] تصميم **صفحة الكتالوج**: شريط التصفية والبحث الجانبي، وعرض بطاقات المنتجات مع مؤشرات توفر المخزون.
- [ ] تصميم **صفحة تفاصيل المنتج**: معرض الصور، مواصفات الخامة، وحاسبة المساحات والكميات التفاعلية.
- [ ] تصميم **سلة عروض الأسعار**: عرض المنتجات المختارة والكميات المحسوبة، ونموذج إدخال بيانات التواصل والتوريد.
- [ ] تصميم **لوحة تحكم المشرف (Admin Interface)**: نماذج إضافة وتعديل المنتجات، وجدول استلام وإدارة عروض الأسعار.
- [ ] بناء نافذة دردشة **مساعد الذكاء الاصطناعي الاستشاري**.

### 3. الربط والبرمجة التفاعلية
- [ ] برمجة منطق حاسبة المقاسات (ضرب الطول × العرض × كمية القطع + نسبة الهدر).
- [ ] بناء نظام إدارة الحالة (State Management) لسلة عروض الأسعار (باستخدام React Context أو Redux).
- [ ] ربط واجهات العميل بـ APIs الخلفية لعرض المنتجات وإرسال عروض الأسعار وحالة الشات بوت.

---

## ⚙️ مهام الخلفية (Backend Tasks)

### 1. إعداد هيكل الخادم وقاعدة البيانات
- [ ] إعداد خادم Node.js باستخدام Express وتجهيز ملف `server.js`.
- [ ] تهيئة الاتصال بقاعدة بيانات MongoDB باستخدام Mongoose.
- [ ] إعداد معالجة الأخطاء وحماية الخادم ضد الهجمات الثغرات الشائعة (CORS, Helmet).
- [ ] حماية مسارات لوحة التحكم باستخدام نظام التحقق وعينات التشفير (JWT - JSON Web Tokens).

### 2. تطوير الموديلات والاستعلامات (Database Models)
- [ ] إنشاء موديل المنتجات `Product Schema` (الاسم، التصنيف، الوصف، الصور، المصدر، المواصفات الفنية).
- [ ] إنشاء موديل طلبات عروض الأسعار `RFQ Schema` (بيانات العميل، المنتجات والكميات المطلوبة، حالة الطلب، تاريخ الطلب).
- [ ] إنشاء موديل المستخدمين والمشرفين `User Schema` لصلاحيات لوحة التحكم.

### 3. بناء واجهات الـ API (Endpoints)
- [ ] تطوير مسارات المنتجات `/api/products` (إضافة، تعديل، حذف، جلب الفلاتر).
- [ ] تطوير مسارات طلبات الأسعار `/api/rfq` (إرسال طلب جديد، جلب الطلبات للإدارة، تعديل حالة الطلب).
- [ ] تطوير مسارات المصادقة `/api/auth` لتسجيل دخول الإدارة.
- [ ] إعداد نظام إرسال الإشعارات بالبريد الإلكتروني للإدارة والعملاء عند تقديم طلب جديد (باستخدام NodeMailer).

---

## 🧠 مهام الذكاء الاصطناعي (AI Tasks)

- [ ] إعداد حساب مطور في منصة Google AI Client أو OpenAI واستخراج مفاتيح الـ API.
- [ ] كتابة وتجربة الـ *System Prompt* المخصص الذي يحدد هوية الروبوت كمستشار خامات وتشطيبات خبير بالمنصة.
- [ ] بناء مسار بالخلفية `/api/ai/chat` يستقبل رسائل المستخدم ويعيد ردود الذكاء الاصطناعي بعد دمجها مع السياق.
- [ ] تحسين نظام الإجابات ليشمل معلومات دقيقة عن منتجات الرخام والحجر الطبيعي والبازلت وسلوكيات استخدامها المعمارية.
- [ ] إعداد آلية لمراقبة استهلاك الـ API والتحكم في كمية الطلبات (Rate Limiting) لمنع سوء الاستخدام.

---

## 🚀 مهام النشر والبيئة (Deployment Tasks)

- [ ] إعداد ملفات التكوين والبيئة مثل `.env` لحفظ متغيرات الربط السرية (رابط MongoDB Atlas، مفتاح الـ AI، وإعدادات البريد).
- [ ] تهيئة مستودع كود للمشروع على GitHub.
- [ ] إنشاء قاعدة بيانات سحابية على منصة MongoDB Atlas وتجهيز عناوين الوصول الآمنة.
- [ ] نشر تطبيق الواجهة الخلفية على منصة Render أو Railway وربط متغيرات البيئة.
- [ ] نشر تطبيق الواجهة الأمامية على منصة Vercel وتوصيله برابط الخلفية المنشور.
- [ ] اختبار أداء المنصة وسرعة الاستجابة وعمل شهادة الحماية SSL لضمان التصفح الآمن.
